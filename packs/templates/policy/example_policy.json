{
    "//": "This is an example policy file. Policies are defined in JSON format.",
    "rules": [
        {
            "effect": "allow",
            "action": "*",
            "resource": "*"
        }
    ]
}
